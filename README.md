This repo impliments a minimal, clean ReAct-style agent in Python you can run directly in Google Colab. It uses an LLM (via OpenAI API) and a simple tool (search/math) also it uses local tools if api key request limit exceeded.

